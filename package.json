{
  "name": "tw2overflow",
  "title": "TWOverflow",
  "description": "Tools for Tribal Wars 2",
  "author": {
    "name": "Relaxeaza",
    "url": "https://gitlab.com/relaxeaza",
    "email": "twoverflow@outlook.com"
  },
  "private": true,
  "version": "1.0.7",
  "dev-version": "2.0.0",
  "license": "WTFPL",
  "repository": {
    "type": "git",
    "url": "https://gitlab.com/relaxeaza/twoverflow"
  },
  "devDependencies": {
    "eslint": "^6.8.0",
    "git-promise": "^0.3.1",
    "glob": "^7.1.6",
    "html-minifier": "^4.0.0",
    "http-server": "^0.12.1",
    "less": "^3.10.3",
    "terser": "^4.4.2"
  },
  "scripts": {
    "make": "node scripts/make.js --lint --minify",
    "make-dev": "node scripts/make.js --dev",
    "make-dev-lint": "node scripts/make.js --dev --lint",
    "release": "node scripts/make.js --lint --minify && node scripts/release.js",
    "release-testing": "node scripts/make.js --lint --minify --dev && node scripts/release.js --testing",
    "server": "http-server ./dist/ --ssl --cert=share/server/cert.pem --key=share/server/key.pem -p 2838"
  }
}
